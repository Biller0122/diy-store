{
  "name": "diy-store",
  "private": true,
  "workspaces": ["apps/web", "apps/web-supplier", "apps/server", "apps/realtime-server", "apps/cms", "apps/admin", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:prod": "npm run build --workspace @diy-store/server && npm run build --workspace @diy-store/web",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:ci": "npm run test --workspace @diy-store/server -- --runInBand && npm run test --workspace @diy-store/web -- --runInBand",
    "test:coverage": "turbo run test:coverage"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
