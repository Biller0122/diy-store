{
  "name": "diy-store",
  "private": true,
  "workspaces": ["apps/web", "apps/server", "apps/cms", "apps/admin", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
