{
  "name": "@diy-store/realtime-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only ../server/src/realtime.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "node ../server/dist/realtime.js"
  },
  "dependencies": {
    "@diy-store/server": "file:../server"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
